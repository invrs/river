#!/usr/bin/env node

var path = require("path")
require("task-env")({
  args: process.argv.slice(2),
  jsonDir: path.resolve(__dirname, "../fixture"),
  tasks: [
    require("../../dist")
  ],
}).catch(console.error)
